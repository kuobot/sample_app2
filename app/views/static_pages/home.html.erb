<% if signed_in? %>

        <%= render 'shared/micropost_form' %>
<div class="container-fluid">
<p>      <%= render 'shared/feed' %>
</p>
<% else %>
  <div class="center hero-unit">
    <h1>Build Your Lifeline</h1>
<br>

    <%= link_to "Start now", signup_path, 
                                class: "btn" %>
  </div></div>
<% end %> 