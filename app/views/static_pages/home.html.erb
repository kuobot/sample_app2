<div class="container-fluid">   
  <div class="row-fluid">
  <div class="well">
    <% if signed_in? %>
      <%= render 'shared/feed' %>
    <% else %>
      <center><h2>Post the things you need... </h2>to the people who care the most.<br><br><%= link_to "Start now", signup_path, class: "btn" %></center>
  </div>
</div>
</div>

<% end %> 