<% if signed_in? %>

  <%= render 'shared/feed' %>

<% else %>

  <div class="center hero-unit">
      <h1>Build Your Lifeline</h1>
      <%= link_to "Start now", signup_path, class: "btn" %>
  </div>

<% end %> 