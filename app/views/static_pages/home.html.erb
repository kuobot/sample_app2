<div class="container-fluid">   
  <div class="well">
    <% if signed_in? %>
      <%= render 'shared/feed' %>
    <% else %>
      <center><h2>Create an eventfeed.</h2><br><%= link_to "Start now", signup_path, class: "btn" %></center>
  </div>
</div>

<% end %> 