
  	<div class="title">
      <%= link_to micropost.title, micropost_path(micropost.id) %>
   	</div>

     <%= mail_to micropost.user.email, "Reply", :subject => "Message from your tethrball from" + ' ' + current_user.name, class: "label" %>
<br>      <%= micropost.content %>
<hr>

<!--   <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span> -->

	  <% if current_user?(micropost.user) %>
    	<br><%= link_to "delete", micropost, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: micropost.content %>
    <%= link_to "edit", edit_micropost_path(micropost.id), @micropost %>


<% end %>
