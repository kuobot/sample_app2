<% provide(:title, @micropost.title) %>


<div class="container-fluid">
  <div class="row-fluid">
    <span class="span12">
      <div class="well">
        <div class="title">
        <%= @micropost.title %>
                </div>
             Posted by <%= link_to @micropost.user.name, user_path(@micropost.user.id) %><hr>
        <%= @micropost.content %>
        <br><br>
        <div class="btn btn-success">Buy ticket</div>
<br>
<br>
    
<br>

    <% if current_user?(@micropost.user) %>


 <% else %>


<% end %>

 <a href="<%= followers_user_path(@micropost.user) %>">
    <strong id="followers" class="stat">
      has <%= @micropost.user.followers.count %> cards
    </strong>
   </a>
  


      </div>
    </span>
  </div>
</div>

