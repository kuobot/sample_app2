<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
  <center><%= f.submit "Uncollect this card", class: "btn btn-large" %></center>
<% end %>