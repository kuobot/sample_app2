<% provide(:title, @user.name) %>

<div class="row"> 
  <aside class="span3"><div class="rectangle">
    <section>
      <h1><center>
        <%= gravatar_for @user %><br>
        <%= @user.name %>
      </h1></center>
    </section>
    <section><center>
       <%= render 'shared/stats' %></section></center>
 <section>
     <%= render 'phone' %>
    </section>
      <br>
      <br>
      <br>
      <br>
          <%= render 'follow_form' if signed_in? %>
        <textarea class="textarea">textarea</textarea>

</div>


  </aside>


  <div class="span8">
  <!--   <%= render 'follow_form' if signed_in? %>
   <% if @user.microposts.any? %>
        <h3>Projects (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
      <%= will_paginate @microposts %>
    <% end %>
  --></div> 
  </div>

