<% provide(:title, @user.name) %>

<div class="image">
        <center><%= image_tag @user.image_url.to_s %></center>

</div>

<div class="container-fluid">
  <div class="row-fluid">
     <div class="title">
<br>

        <%= @user.name %>

        <%= render 'follow_form' if signed_in? %>
        <p>
     </div>



        <%= @user.description %>
        </p>



    <%= render 'shared/stats' %> 

<p><p><hr>
<p><p>


        <% if @user.microposts.any? %>

           <%= render @user.microposts %>

         <%= will_paginate @microposts %>
        <% else %>

          <div class="empty">
        <% if current_user?(@user) %>
          <center>You don't have any  <br><br><%= link_to 'Upload one now', help_path, class: "btn btn-success" %></center>
        <% else %>

        <% end %>
          </div>
  </div>
</div>
<% end %>
      