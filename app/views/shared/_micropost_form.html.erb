

<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>


 <center>
     <%= f.text_area :title, placeholder: "Title of event", class: "textinput" %>
 <br><%= f.text_area :content, placeholder: "Write a description of the event here...", class: "textfield" %><br>
<%= f.submit "           Broadcast locally         ", class: "basic-cancel-button" %></center>
<% end %>