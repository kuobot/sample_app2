
    <div id="<%= feed_item.id %>" class="feed-item <%= feed_item.is_today ? 'today' : '' %>">    </div>

      <div class="title">
        <%= link_to feed_item.title, micropost_path(feed_item.id) %>

      </div> <i>Posted by
<%= link_to feed_item.user.name, feed_item.user %></i>

<!--   -- <%= feed_item.user.name %>
 -->        

<!--       <p><%= link_to feed_item.user.name, feed_item.user %><br><br>
        <%= feed_item.content %> -->
      </p>
  </span>


<!--        <span class="timestamp">  
      Asked <%= time_ago_in_words(feed_item.created_at) %> ago:  </span><br>
<%= link_to gravatar_for(feed_item.user), feed_item.user %>
     <span class="user">
      <%= link_to feed_item.user.name, feed_item.user %>
    </span> -->

    <hr>

<!--  <% if current_user?(feed_item.user) %>
  <%= link_to "delete", feed_item, method: :delete,
                                   data: { confirm: "You sure?" },
                                   title: feed_item.content %>
<% end %> -->

