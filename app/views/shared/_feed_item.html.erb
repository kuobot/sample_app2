<li id="<%= feed_item.id %>" class="<%= feed_item.is_today ? 'today' : '' %>">

        <span class="timestamp">  
      Asked <%= time_ago_in_words(feed_item.created_at) %> ago:  </span><br>
<%= link_to gravatar_for(feed_item.user), feed_item.user %>
   <!--  <span class="user">
      <%= link_to feed_item.user.name, feed_item.user %>
    </span> -->
    <span class="content"><%= feed_item.content %></span>
<br>
<!--       <span class="signature"><%= link_to feed_item.user.name, feed_item.user %>.<br>     </span> 
 --> <!--      <span class="label"> <%= mail_to feed_item.user.email, "Reply", :subject => "Message from your tethrball from" + ' ' + feed_item.user.name %> -->

      <%= mail_to feed_item.user.email, " ", :subject => "Message from your tethrball re:" + ' ' + feed_item.content %></b></a>
</span> 
 <!--  <% if current_user?(feed_item.user) %>
    <%= link_to "delete", feed_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feed_item.content %>
  <% end %> -->
</li>
</div>